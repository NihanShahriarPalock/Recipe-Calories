import PropTypes from "prop-types"

const Cooking = ({blog}) => {
    const {recipe_name}=blog;
    return (
        <div>
            
            
        </div>
    );
};
Cooking.propTypes={
    blog:PropTypes.object
}
export default Cooking;